/**
 * Standalone command-line utilities, not wired into the Spring web app.
 * These run against local files the user provides and are invoked
 * directly (e.g. via the Maven exec plugin), never from a web request.
 */
package com.vinoigitare.tools;
