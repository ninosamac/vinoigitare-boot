/**
 * Persistence layer (flat-file {@code .tab} storage today, embedded DB later).
 * Ported from the old {@code Vinoigitare_TextFileStorage} module. Populated in Phase 1.
 */
package com.vinoigitare.storage;
